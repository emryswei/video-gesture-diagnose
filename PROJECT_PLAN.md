# SOP 视频流程审核项目方案

## 1. 目标

输入一段用户上传的视频，按照预先定义的标准流程（Standard Operating Procedure, SOP）判断每个步骤是否完成且动作是否正确，并输出可复核的 JSON 结果。

首个场景为 WHO 酒精免洗手手部消毒流程（hand rubbing），但系统设计应支持以后增加其他标准流程。

本项目定位为单机 Demo，用于验证 SOP 视频审核流程、模型能力和交互方式，不以生产上线为目标。设计优先精简、可运行和可演示，不引入多用户、分布式、高可用或企业级运维能力。

V1 的界面、状态、错误信息和模型证据统一使用英文，不实现语言切换。V1.1 增加简体中文；项目语言集合固定为 `en` 和 `zh-CN`，不支持繁体中文或其他语言。

## 2. 总体处理流程

```text
用户上传视频
  ↓
视频校验：格式、时长、文件大小、可解码性
  ↓
创建异步分析任务，返回任务 ID
  ↓
整段视频按 2 FPS 均匀抽帧，最多 96 帧并保留时间戳
  ↓
视觉语言模型（VLM）一次调用判断流程边界与各步骤
  ↓
规则引擎检查结果完整性、顺序和时长
  ↓
输出逐步结果、时间证据、摘要和总体状态
```

## 3. SOP 配置文件

每一种流程都应使用独立、版本化的配置文件作为唯一标准来源，而不是把规则写死在提示词或代码中。

示例字段：

```json
{
  "id": "who_handrub",
  "standard_version": "WHO-2009",
  "definition_version": 1,
  "title": "WHO alcohol-based handrub",
  "duration_min_seconds": 20,
  "duration_max_seconds": 30,
  "steps": [
    {
      "id": "palm_to_palm",
      "order": 2,
      "label": "掌心相对揉搓",
      "instruction": "双掌相对并来回揉搓。",
      "required": true,
      "requires_both_sides": false
    }
  ]
}
```

建议保留以下信息：

- SOP ID、来源、版本、生效日期；
- 步骤 ID、名称、顺序、动作描述、左右手要求；
- 是否必需；
- 流程总时长范围；
- 后续扩展字段：重复次数、单步持续时间、允许变体、严重程度。

外部标准改变时更新 `standard_version`；系统对同一标准的解释改变时递增 `definition_version`。不要覆盖旧定义，确保历史报告可追溯。

### 3.1 WHO hand-rub V1 Auditable Steps

| 顺序 | 步骤 ID | 名称 | V1 通过条件 | 要求双侧 |
|---:|---|---|---|:---:|
| 1 | `apply_product` | Apply product | 清楚看到消毒产品进入手掌并开始覆盖双手 | 否 |
| 2 | `palm_to_palm` | Palm to palm | 清楚看到双掌相对来回揉搓 | 否 |
| 3 | `palm_over_dorsum` | Palm over dorsum | 掌心覆盖另一只手背并且手指交叉 | 是 |
| 4 | `palms_fingers_interlaced` | Fingers interlaced | 双掌相对且手指交叉揉搓 | 否 |
| 5 | `backs_of_fingers` | Backs of fingers | 弯曲指背接触并揉搓另一掌心 | 是 |
| 6 | `thumbs` | Thumb rubbing | 拇指被另一只手掌旋转包裹揉搓 | 是 |
| 7 | `fingertips_in_palm` | Fingertips in palm | 指尖在另一掌心旋转揉搓 | 是 |

V1 只要求动作清楚出现，不检查重复次数或单步持续时间。要求双侧的步骤只有两侧都获得充分证据才为 `passed`；明确只完成一侧或动作形态明显错误为 `failed`；采样遗漏、遮挡或画面不清为 `uncertain`。

## 4. V1：最小可行版本（MVP）

### 4.1 范围

V1 只解决一个问题：**输入一个短视频，判断每一个规定步骤是否完整、动作是否正确，并检查流程总时长。**

当 SOP 明确要求双侧动作时，左右手都获得充分视觉证据才算步骤完整。V1 暂不处理重复次数、复杂动作顺序惩罚、单步持续时间、实时摄像头提示或多 SOP 管理。

WHO hand-rub V1 包含 7 个 Auditable Steps：涂抹产品以及 6 种揉搓动作。干燥不作为 SOP 步骤或评分项，因为普通视频无法可靠验证皮肤是否干燥；结果通过非评分警告说明这一限制。

### 4.2 输入约束

- 支持 MP4、MOV、WebM 等常见格式；
- 建议视频长度为 15–60 秒；
- 双手应保持在画面内，画面清晰、背景简单；
- 上传前限制文件大小和最大时长；
- V1 可要求固定拍摄角度，避免先处理任意角度视频。
- V1 不持久化上传视频或抽帧；后端临时副本在分析结束后立即删除。

### 4.3 视频读取策略

V1 使用单次分析管线：

1. 对 15–60 秒的整段视频按约 2 FPS 均匀抽帧。
2. 最多保留 96 帧；超过上限时在整段时间范围内均匀选择。
3. 每帧保留相对源视频的时间戳。
4. 将帧与 SOP 一次发送给模型，由模型同时定位流程起止时间并判断步骤。

如果无法可靠定位流程区间或步骤，结果应标记为 `uncertain`，并要求重新录制或人工复核。V1 不自动发起第二次模型分析。

### 4.4 模型输出协议

模型必须只输出结构化 JSON。每个 SOP 步骤必须返回一条结果：

```json
{
  "sop_id": "who_handrub",
  "standard_version": "WHO-2009",
  "definition_version": 1,
  "summary": "发现大多数动作，但步骤 5 未获得足够视觉证据。",
  "steps": [
    {
      "step_id": "palm_to_palm",
      "step_order": 2,
      "step_label": "掌心相对揉搓",
      "status": "passed",
      "confidence": 0.86,
      "start_sec": 5.2,
      "end_sec": 7.8,
      "observation": "可见双掌相对并持续来回揉搓。"
    }
  ]
}
```

状态定义：

- `passed`：视频中有充分证据表明动作已正确完成；
- `failed`：视频中有充分证据表明动作被跳过或明显错误；
- `uncertain`：画面遮挡、模糊、片段缺失或证据不足，不能猜测为失败。

### 4.5 V1 判定逻辑

- 模型负责识别和描述动作；
- 程序负责校验 JSON、确保每一步都有结果；
- 程序从本次审核使用的 SOP 定义中把 `step_order` 和 `step_label` 写入结果；模型不得生成或改写这些字段；
- 双侧步骤只有左右两侧都可见且正确完成时才为 `passed`；只完成一侧为 `failed`，无法确认另一侧为 `uncertain`；
- 程序根据检测到的流程开始和结束时间计算总时长；超出 SOP 范围为 `failed`，无法可靠定位边界为 `uncertain`；
- V1 的总体结果：所有必需步骤为 `passed` 且总时长合规才通过；任意明确失败则失败；没有明确失败但存在 `uncertain` 则整体为不确定；
- 不输出或使用浮点 `score`；展示层直接从必需步骤计算通过数与总数，`uncertain` 不计部分分；
- 每项结果必须显示时间范围和证据描述，方便人工复看。
- 结果警告包含 `Dryness cannot be verified visually.`，但该警告不改变评分状态。

### 4.6 V1 交付物

- 视频上传页面或 API；
- 单一 WHO hand-rub SOP 配置文件；
- 视频抽帧与时间戳模块；
- VLM 推理模块；
- JSON 解析与规则校验；
- 从版本化 SOP 定义生成步骤名称与顺序，不在 UI 中重复维护；
- 双侧步骤完整性与流程总时长校验；
- 逐步结果页面/API 响应；
- 一组人工标注测试视频与基础自动测试。

V1 的浏览器页面使用用户所选本地文件播放证据视频。分析结果与视频不跨页面刷新恢复；历史记录和持久化存储不属于 V1。

V1 使用单个内存异步任务槽。上传接口立即返回 `analysis_id`，UI 查询 `queued`、`analyzing`、`succeeded` 或 `failed` 状态；同一时间只允许一个任务处于 `queued` 或 `analyzing`，再次提交返回 HTTP `409 Conflict` 和 `analysis_in_progress`。V1 不实现多任务排队、取消、优先级或自动重试。成功或失败的任务结果在内存中保留 30 分钟后过期，任务元数据不作为历史记录持久化；过期查询返回 HTTP `410 Gone`。

## 5. 后续版本路线图

### V1.1：提高基础可靠性

- 增加简体中文界面与简体中文模型证据，保留英文为默认语言；项目只支持 `en` 和 `zh-CN`；
- 将分析任务与结果从 30 分钟内存保留升级为本地持久化；
- 每条 Audit Record 保存分析 JSON、SOP Identity、模型与提示词版本、原文件名、文件哈希和创建时间；
- 原始视频仅在用户明确选择保留时保存；未保留视频的历史结果不提供证据回放；
- 不单独持久化抽帧，需要时从已保留的原始视频重新生成；
- 提供删除单条 Audit Record 和清空本地历史的能力；
- Audit Record 不自动过期；用户主动删除记录时同时删除其关联视频，清空本地历史前必须确认；
- 历史页面显示本地记录与视频占用空间；
- Audit Record、任务状态和版本信息存入 SQLite；原始视频作为普通文件保存在系统应用数据目录；
- SQLite 只保存视频内容哈希、相对路径和大小，不保存视频 BLOB 或项目目录内的绝对路径；
- 每条 Audit Record 最多对应一个以 `analysis_id` 命名的视频文件；删除记录时直接删除该文件；
- SHA-256 内容哈希仅用于标识视频来源，不用于文件去重或引用计数；
- 检查步骤顺序；
- 增加低帧率粗扫描以定位候选流程区间；
- 对 `uncertain` 步骤自动进行局部高帧率二次分析；
- 允许用户查看关键帧和对应时间点；
- 增加重新录制提示。

### V2：动作细节审核

- 判断某动作是否达到规定重复次数；
- 判断每一步持续时间；
- 识别特定错误姿势；
- 使用手部关键点提高双侧动作判断精度；
- 支持可配置的步骤替代动作与可交换步骤。

此阶段可加入手部关键点检测（hand pose estimation），与 VLM 的语义判断结合：

```text
VLM：识别动作语义、流程上下文和证据说明
手部关键点：判断左右手、手指/拇指姿态、动作重复
规则引擎：计算顺序、时长、次数和最终结论
```

### V3：产品化与扩展

- 支持多个 SOP、版本管理和后台配置；
- 实时摄像头引导；
- 用户账号、历史报告和趋势分析；
- 人工复核队列；
- 批量上传与导出报告；
- 模型、提示词和 SOP 版本审计；
- 云端 GPU 队列、任务状态和并发控制。

## 6. 数据与评估

V1 使用 24 条人工标注视频作为固定 Demo 测试集：

- 6 条全部正确；
- 7 条分别漏掉或错误执行一个不同步骤；
- 4 条单侧未完成或手部遮挡；
- 3 条总时长过短或过长；
- 4 条覆盖不同光线、背景或拍摄角度。

每条视频人工记录：步骤状态、开始/结束时间、失败原因、是否可见。

V1 验收标准：

- 24 条视频全部返回合法结构化结果且分析流程不崩溃；
- 整体 `passed` / `failed` / `uncertain` 准确率至少 80%；
- 七个步骤合计判定准确率至少 80%；
- 明确不合规的视频最多允许 1 条被错误判为 `passed`；
- 步骤时间定位误差中位数不超过 2 秒；
- 记录推理耗时，但不设置硬性门槛，因为耗时取决于外部模型服务。

不要只看“总体通过率”，因为它可能掩盖某一个关键步骤经常被误判的问题。

## 7. 录制协议

受控拍摄通常比增大模型更有效。建议在上传界面提示用户：

- 开始前双手静止约 1 秒；
- 操作过程中双手完整可见，避免遮挡；
- 使用稳定镜头、充足光线和简单背景；
- 不要快进、剪辑或频繁移动镜头；
- 完成后双手静止约 1 秒。

这些规则能帮助系统定位流程边界，并降低模型把“没看见”误判为“没做”的风险。

## 8. 模型与部署注意事项

- 当前 4 GB AMD 显存不适合在应用进程内稳定运行 Qwen3-VL-4B 视频推理；
- Demo 应用负责视频校验、抽帧、异步任务和规则判断，不在 FastAPI 进程中加载模型权重；
- Qwen3-VL 部署为独立 GPU 模型服务，应用通过 OpenAI-compatible API 发送带时间戳的抽帧和提示词；
- 通过 `MODEL_BASE_URL`、`MODEL_API_KEY` 和 `MODEL_NAME` 配置模型服务；V1 不承诺离线运行；
- 限制视频时长、帧数、分辨率、上下文长度和并发数；
- 保存模型版本、量化方式、提示词版本和 SOP 版本，确保结果可重现；
- 模型置信度不是实际正确率，需要通过人工标注数据校准。

## 9. 隐私与人工复核

- 视频可能包含脸部、声音、工作环境或其他敏感信息；
- V1 后端仅在推理期间保存临时视频副本，分析完成或失败后立即删除，不持久化抽帧；
- 浏览器中的本地视频地址仅在当前页面会话内有效，离开页面时释放；
- V1.1 开始在本地持久化分析任务与结果；数据保留至用户主动删除，访问授权仍需在实现前明确；
- V1.1 始终保存 Audit Record；原始视频默认不保存，只有用户明确选择时才进入本地历史；抽帧不单独持久化；
- 删除 Audit Record 时同步删除其关联视频；清空本地历史需要确认，并显示当前本地存储占用；
- 云端推理前必须说明数据是否离开本机；
- V1 上传页面明确说明抽取的视频帧会发送到所配置的模型服务，原始视频不会作为整体上传给模型服务；
- 对 `uncertain`、低置信度或高风险失败结果提供人工复核；
- 系统应定位为辅助审核工具，不应在未验证的情况下作为医疗、认证或处罚的唯一依据。

## 10. 推荐实施顺序

1. 确定 WHO hand-rub V1 的步骤定义、双侧要求、总时长范围与录制协议。
2. 准备固定的 24 条人工标注 Demo 测试视频。
3. 实现上传、整段 2 FPS 均匀抽帧和单次多图模型分析。
4. 实现严格 JSON 输出、双侧步骤完整性、总时长校验和结果展示。
5. 在标注集上评估每一步准确率，记录失败案例。
6. 优化抽帧策略与提示词，再开始 V1.1 的顺序/时长审核。
